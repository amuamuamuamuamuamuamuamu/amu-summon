const $=id=>document.getElementById(id),params=new URLSearchParams(location.search),room=params.get("room"),key=`amu-summon-${room||"1"}`;let data=null;const pick=a=>a[Math.floor(Math.random()*a.length)],clamp=n=>Math.max(1,Math.min(100,n));
function newMonster(){return{monster:1+Math.floor(Math.random()*65),stage:1,level:1,name:pick(GAME_DATA.names),feeling:pick(GAME_DATA.feelings),stats:GAME_DATA.stats.slice().sort(()=>Math.random()-.5).slice(0,4).map(name=>({name,value:1+Math.floor(Math.random()*30)})),log:[]}}
function save(){localStorage.setItem(key,JSON.stringify(data))}function show(id){["admin","start","game"].forEach(x=>$(x).classList.toggle("hidden",x!==id))}
function render(){show("game");$("playerName").textContent=data.breeder;$("monsterName").textContent=data.name;$("monster").src=`進化モンスター/モンスター${data.monster}/${data.stage}.png`;$("level").textContent=`レベル ${data.level}`;$("stage").textContent=`第${data.stage}段階`;$("feeling").textContent=`今の気持ち：${data.feeling}`;$("stats").innerHTML=data.stats.map(s=>`<div class="stat"><div>${s.name}：${s.value}</div><div class="bar"><div class="fill" style="width:${s.value}%"></div></div></div>`).join("");$("evolve").disabled=data.stage>=3;$("evolve").textContent=data.stage>=3?"最終進化済み":"進化させる";$("log").innerHTML=(data.log||[]).slice(-6).reverse().map(x=>`・${x}`).join("<br>")||"召喚されたばかりです。"}
document.querySelectorAll(".room").forEach(b=>b.addEventListener("click",()=>location.href=`?room=${b.dataset.room}`));$("player").addEventListener("input",e=>$("summon").disabled=!e.target.value.trim());$("summon").addEventListener("click",()=>{data=newMonster();data.breeder=$("player").value.trim();save();render();$("notice").textContent="召喚しました！"});$("replace").addEventListener("click",()=>{const breeder=data.breeder;data=newMonster();data.breeder=breeder;save();render()});$("evolve").addEventListener("click",()=>{if(data.stage>=3)return;data.stage++;data.level++;data.stats.forEach(s=>s.value=clamp(s.value+5+Math.floor(Math.random()*16)));data.feeling=pick(GAME_DATA.feelings);data.log.push(`${data.name}が第${data.stage}段階へ進化しました。`);save();render();$("notice").textContent="進化しました！"});$("backAdmin").addEventListener("click",()=>location.href=location.pathname);$("gameAdmin").addEventListener("click",()=>location.href=location.pathname);
if(room){show("start");try{data=JSON.parse(localStorage.getItem(key));if(data)render()}catch{}}else show("admin");
